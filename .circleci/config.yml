# Java Gradle CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2.1
machine: true
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01

    resource_class: large

    working_directory: ~/repo

    environment:
      # Customize the JVM maximum heap limit
      JVM_OPTS: -Xmx3200m
      TERM: dumb

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "build.gradle" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: ./gradlew dependencies
      - save_cache:
          paths:
            - ~/.gradlew
          key: v1-dependencies-{{ checksum "build.gradle" }}

      # Run tests!
      - run: ./gradlew clean build -x dokkaJavadoc
      - run: docker network prune
      # Store test results
      - store_test_results:
          path: corda-testacles-cordapp-example/build/test-results

      # Store test reports
      #- store_artifacts:
      #    path: corda-testacles-cordapp-example/build/reports
      #    destination: testreport
      # Store generated nodes
      #- store_artifacts:
      #    path: corda-testacles-cordapp-example/build/testacles
      #    destination: testacles

